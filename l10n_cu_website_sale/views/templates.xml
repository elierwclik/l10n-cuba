<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <template id="l10n_cu_address_form_fields" inherit_id="portal.address_form_fields" name="Address Details">
        <xpath expr="//div[@id='div_state']" position="after">
            <div class="w-100"/>
            <div id="div_municipality" class="col-lg-6 mb-2"
                 t-att-style="not state_municipalities and 'display: none'">
                <label class="col-form-label" for="o_res_municipality_id">Municipality</label>
                <select id="o_res_municipality_id" name="res_municipality_id" class="form-select">
                    <option value="">Municipality...</option>
                    <t t-foreach="state_municipalities" t-as="m">
                        <option t-att-value="m.id" t-att-selected="m.id == res_municipality_id">
                            <t t-esc="m.name"/>
                        </option>
                    </t>
                </select>
            </div>
        </xpath>
    </template>

</odoo>
